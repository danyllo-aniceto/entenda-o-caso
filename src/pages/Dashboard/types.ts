export interface IForm {
  publicationDate: string;
  caseDescription: string;
  contextInformation: string;
  partFont: string;
  importanceCase: string;
  linkYoutube: string;
}

export interface ITooltipProps {
  visible: boolean;
}
